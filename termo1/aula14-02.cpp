#include <iostream> //o include serve para incluir uma biblioteca do C no seu código
#include <locale>
using namespace std;//isso serve para incluir uma biblioteca tambem, porem com outro nome
main()//abrir a main para começar as linhas de codigo(no caso a parte principal)
{
    setlocale(LC_ALL, "Portuguese");//para aplicar o teclado padrão do portugues

    //Soma de dois numeros, feito em aula
    /*int num1,num2,soma;
    cout<<"Digite o valor 1:\n";
    cin>>num1;
    cout<<"Digite o valor 2:\n";
    cin>>num2;
    cout<<"Soma dos numeros = "<<(num1+num2);
*/
   
   
   //Conversão de dolar para real, Feito na aula de algoritmos 1
   /*float cot_dolar, valor;
   cout<<"Digite a cotacao do dolar";
   cin>>cot_dolar;
   cout<<"Digite o valor em dolar";
   cin>>valor;
   cout<<(cot_dolar*valor);*/

  
  
  //Calcular area do quadrado
    /*int lado, area;
    cout<<"Digite o lado: ";
    cin>>lado;
    cout<<"A área é = "<<(lado*lado);
    */
}